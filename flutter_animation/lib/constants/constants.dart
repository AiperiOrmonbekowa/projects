import 'package:flutter/material.dart';

const bgColor = Color(0xFFF6F5F2);
const primaryColor = Color(0xFF4BA944);

const defaultPadding = 20.0;
const cardBarWeight = 100.0;
const headerWeight = 85.0;

const panelTranslition = Duration(milliseconds: 500);
